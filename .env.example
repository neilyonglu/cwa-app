# 本機開發用環境變數。
# 複製成 .env（已 gitignore，不會 commit）後填入真實值。
#
# 一鍵設定（從 cwa-tg-bot 同步）：
#   cp ../cwa-tg-bot/.env .env

# Gemini 雨勢分析的 API key（Google AI Studio 申請）。
# 沒設的話 endpoint 仍會正常運作，aiAnalysis 欄位回傳 null。
GEMINI_API_KEY=
